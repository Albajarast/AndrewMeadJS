let isRaining = true;

isRaining = false;

console.log(isRaining);

const myConstant = 'I am a constant';

// myConstant = 'I will give an error';

console.log(myConstant);

const person = {
    age: 35
};

// elements of an const object can be modified by accesgin to
person.age = 36

console.log(person);
